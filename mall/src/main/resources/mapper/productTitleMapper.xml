<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- product_title -->
<mapper namespace="com.ppx.mall.dao.ProductTitleDao">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="productTitleMap" type="ProductTitle">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="title" property="title" jdbcType="VARCHAR" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        pt.id, pt.title
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->

    <!-- 查询所有 -->
    <select id="findProductTitleAll" resultMap="productTitleMap">
        SELECT
            <include refid="allColumns" />
        FROM product_title pt
    </select>

    <!-- 根据条件参数查询列表 -->
    <select id="findProductTitleByTitleName" resultType="int" parameterType="String">
        SELECT id FROM product_title pt WHERE title=#{title}
    </select>

    <!-- 根据主键查询信息 -->
    <select id="findProductTitleByIds" resultMap="productTitleMap" parameterType="list">
        SELECT
            <include refid="allColumns" />
        FROM product_title pt WHERE pt.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 新增信息 -->
    <insert id="addProductTitle">
        INSERT INTO product_title (
            id, title
        ) VALUES (
            #{id},
            #{title}
        )
    </insert>

    <!-- 批量新增信息 -->
    <insert id="addProductTitles" parameterType="list">
        INSERT INTO product_title (
            id, title
        ) VALUES
        <foreach collection="list" index="index" item="item" separator=",">
            (
                #{item.id},
                #{item.title}
            )
        </foreach>
    </insert>

    <!-- 修改信息 -->
    <update id="updateProductTitle">
        UPDATE product_title
        <set>
            <if test="title != null">
                title = #{title}
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 批量修改信息 -->
    <update id="updateProductTitles" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE product_title
            <set>
                <if test="item.title != null">
                    title = #{item.title}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>

    <!-- 根据主键删除 -->
    <delete id="deleteProductTitleById" parameterType="int">
        DELETE FROM product_title WHERE id = #{id}
    </delete>

    <!-- 根据主键批量删除 -->
    <delete id="deleteProductTitleByIds" parameterType="list">
        DELETE FROM product_title WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
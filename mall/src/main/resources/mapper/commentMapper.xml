<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- comment -->
<mapper namespace="com.ppx.mall.dao.CommentDao">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="commentMap" type="Comment">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="productId" property="productId" jdbcType="BIGINT" />
        <result column="account" property="account" jdbcType="VARCHAR" />
        <result column="content" property="content" jdbcType="VARCHAR" />
        <result column="time" property="time" jdbcType="BIGINT" />
        <result column="type" property="type" jdbcType="INTEGER" />
        <result column="thumb" property="thumb" jdbcType="INTEGER" />
        <result column="responseId" property="responseId" jdbcType="BIGINT" />
        <result column="responseTwoId" property="responseTwoId" jdbcType="BIGINT" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        c.id, c.productId, c.account, c.content, c.time, c.type, c.thumb, c.responseId, 
        c.responseTwoId
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->

    <!-- 查询所有 -->
    <select id="findCommentAll" resultMap="commentMap">
        SELECT
            <include refid="allColumns" />
        FROM comment c
    </select>

    <!-- 根据条件参数查询列表 -->
    <select id="findCommentByCondition" resultMap="commentMap" parameterType="map">
        SELECT
            <include refid="allColumns" />
        FROM comment c WHERE 1 = 1
        <if test="productId != null">
            AND c.productId = #{productId}
        </if>
        <if test="account != null and account != ''">
            AND c.account = #{account}
        </if>
        <if test="content != null and content != ''">
            AND c.content = #{content}
        </if>
        <if test="time != null">
            AND c.time = #{time}
        </if>
        <if test="type != null">
            AND c.type = #{type}
        </if>
        <if test="thumb != null">
            AND c.thumb = #{thumb}
        </if>
        <if test="responseId != null">
            AND c.responseId = #{responseId}
        </if>
        <if test="responseTwoId != null and responseTwoId != ''">
            AND c.responseTwoId =#{responseTwoId}
        </if>
    </select>

    <!-- 根据主键查询信息 -->
    <select id="findCommentById" resultType="Comment" parameterType="long">
        SELECT
          <include refid="allColumns"/>
        FROM comment c WHERE c.id=#{id}
    </select>
    <select id="findCommentByIds" resultMap="commentMap" parameterType="list">
        SELECT
            <include refid="allColumns" />
        FROM comment c WHERE c.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 新增信息 -->
    <insert id="addComment">
        INSERT INTO comment (
            id, productId, account, content, time, type, thumb, responseId, 
            responseTwoId
        ) VALUES (
            #{id},
            #{productId},
            #{account},
            #{content},
            #{time},
            #{type},
            #{thumb},
            #{responseId},
            #{responseTwoId}
        )
    </insert>

    <!-- 批量新增信息 -->
    <insert id="addComments" parameterType="list">
        INSERT INTO comment (
            id, productId, account, content, time, type, thumb, responseId,
        responseTwoId
        ) VALUES
        <foreach collection="list" index="index" item="item" separator=",">
            (
                #{item.id},
                #{item.productId},
                #{item.account},
                #{item.content},
                #{item.time},
                #{item.type},
                #{item.thumb},
                #{item.responseId},
                #{item.responseTwoId}
            )
        </foreach>
    </insert>

    <!-- 修改信息 -->
    <update id="updateComment" parameterType="Comment">
        UPDATE comment
        <set>
            <if test="productId != null">
                productId = #{productId},
            </if>
            <if test="account != null">
                account = #{account},
            </if>
            <if test="content != null">
                content = #{content},
            </if>
            <if test="time != null">
                time = #{time},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="thumb != null">
                thumb = #{thumb},
            </if>
            <if test="responseId != null">
                responseId = #{responseId},
            </if>
            <if test="responseTwoId != null">
                responseTwoId = #{responseTwoId}
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 批量修改信息 -->
    <update id="updateComments" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE comment
            <set>
                <if test="item.productId != null">
                    productId = #{item.productId},
                </if>
                <if test="item.account != null">
                    account = #{item.account},
                </if>
                <if test="item.content != null">
                    content = #{item.content},
                </if>
                <if test="item.time != null">
                    time = #{item.time},
                </if>
                <if test="item.type != null">
                    type = #{item.type},
                </if>
                <if test="item.thumb != null">
                    thumb = #{item.thumb},
                </if>
                <if test="item.responseId != null">
                    responseId = #{item.responseId},
                </if>
                <if test="item.responseTwoId != null">
                    responseTwoId = #{item.responseTwoId}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>

    <!-- 根据主键删除 -->
    <delete id="deleteCommentById" parameterType="long">
        DELETE FROM comment WHERE id = #{id}
    </delete>

    <!-- 根据主键批量删除 -->
    <delete id="deleteCommentByIds" parameterType="list">
        DELETE FROM comment WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
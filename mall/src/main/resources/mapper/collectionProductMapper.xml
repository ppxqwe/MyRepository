<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- collection_product -->
<mapper namespace="com.ppx.mall.dao.CollectionProductDao">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="collectionProductMap" type="CollectionProduct">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="account" property="account" jdbcType="VARCHAR" />
        <result column="productId" property="productId" jdbcType="BIGINT" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        cp.id, cp.account, cp.productId
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->

    <!-- 查询所有 -->
    <select id="findCollectionProductAll" resultMap="collectionProductMap">
        SELECT
            <include refid="allColumns" />
        FROM collection_product cp
    </select>

    <!-- 根据条件参数查询列表 -->
    <select id="findCollectionProductByCondition" resultMap="collectionProductMap" parameterType="map">
        SELECT
            <include refid="allColumns" />
        FROM collection_product cp WHERE 1 = 1
        <if test="account != null">
            AND cp.account =#{account}
        </if>
        <if test="productId != null">
            AND cp.productId = #{productId}
        </if>
        <if test="start!=null and count!=null">
            LIMIT #{start},#{count}
        </if>
    </select>

    <!-- 根据主键查询信息 -->
    <select id="findCollectionProductByIds" resultMap="collectionProductMap" parameterType="list">
        SELECT
            <include refid="allColumns" />
        FROM collection_product cp WHERE cp.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    <select id="findCollectionProductById" resultType="CollectionProduct" parameterType="long">
        SELECT
        <include refid="allColumns" />
        FROM collection_product cp WHERE cp.id = #{id}
    </select>

    <!-- 新增信息 -->
    <insert id="addCollectionProduct" parameterType="CollectionProduct">
        INSERT INTO collection_product (
            id, account, productId
        ) VALUES (
            #{id},
            #{account},
            #{productId}
        )
    </insert>

    <!-- 批量新增信息 -->
    <insert id="addCollectionProducts" parameterType="list">
        INSERT INTO collection_product (
            id, account, productId
        ) VALUES
        <foreach collection="list" index="index" item="item" separator=",">
            (
                #{item.id},
                #{item.account},
                #{item.productId}
            )
        </foreach>
    </insert>

    <!-- 修改信息 -->
    <update id="updateCollectionProduct">
        UPDATE collection_product
        <set>
            <if test="account != null">
                account = #{account},
            </if>
            <if test="productId != null">
                productId = #{productId}
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 批量修改信息 -->
    <update id="updateCollectionProducts" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE collection_product
            <set>
                <if test="item.account != null">
                    account = #{item.account},
                </if>
                <if test="item.productId != null">
                    productId = #{item.productId}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>

    <!-- 根据主键删除 -->
    <delete id="deleteCollectionProductById" parameterType="long">
        DELETE FROM collection_product WHERE id = #{id}
    </delete>

    <!-- 根据主键批量删除 -->
    <delete id="deleteCollectionProductByIds" parameterType="list">
        DELETE FROM collection_product WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>